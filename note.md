Build the operation:


to switch the QEMU debug console:
Ctrl+A then C  
exit:
press q and enter



kernel:
boot() -> set stack pointer -> jump to kernel_main() -> clear .bss -> loop forever

2. external linker symbols:
